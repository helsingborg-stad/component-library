@element([
    'classList' => $classList,
    'attributeList' => $attributeList
])
    @element([
        'id' => $id,
        'attributeList' => $mapAttributeList
    ])
    <!-- Map -->
    @endelement
    <!-- Map Container -->
@endelement